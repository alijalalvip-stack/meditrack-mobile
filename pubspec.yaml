name: meditrack_mobile
version: 1.0.0+1

description: A new Flutter project.

publish_to: 'none' # Remove this line if the package is meant to be published

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true